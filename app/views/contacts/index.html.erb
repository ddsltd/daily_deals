<h1> Listing Contacts </h1>
<ul>
	<% @contacts.each do |contact| %>
		<%= content_tag_for(:li,contact,:data=>{:name => contact.name, :phone => contact.phone }) do %>
			<%= contact.name %> from <%=contact.phone %>
			<% end %>	
	<% end %>	
</ul>
<div id="tip" style='display:none'>
</div>